export interface FeatureItem {
  icon: string;
  title: string;
  description: string;
  colorClass: string;
  iconBgClass: string;
}

export interface TestimonialData {
  quote: string;
  author: string;
  avatarUrl: string;
  rating: number;
}
